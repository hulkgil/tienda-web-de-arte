name: Greetings

on: [pull_request, issues]

jobs:   actions / saludar
    - lanzamiento / *
  hablar+ saludar :
    runs-on: ubuntu-latest 
    steps : acciones / configuración de nodos @ v1          
    - uses: actions/first-interaction@v1
      with: construir : acciones / configuración de nodos @ v1           
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Message that will be displayed on users'' first issue'
        pr-message: 'Message that will be displayed on users'' firstsonre
 
